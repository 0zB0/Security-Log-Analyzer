# Zeek stable-endpoint retry indicator

Six failed TCP connections from one source to one destination endpoint inside five minutes. This
is a low-confidence retry-loop indicator, not proof of command and control.
